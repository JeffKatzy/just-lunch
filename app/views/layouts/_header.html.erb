<% if current_user %>
	Hello, <%= current_user.name %>!
	 <%=link_to "Log Out",logout_path %>
	 <%=link_to "Meetings",user_meetings_path(current_user) %>
<% else %>
  <%=link_to "Sign Up", signup_path %>
	<%=link_to "Log In", login_path %>
<% end %>

<p id="notice"><%= notice %></p>
